
Easy and Fast usage:

simply start your php script with:
require(path to easymail.php);
and then you are ready to use 
email($to,$subject,$message,$is_html);

Configuration

Open config.json
Enter all credentials and network settings.
And you are ready to go!

THIS ONLY WORKS FOR WINDOWS
